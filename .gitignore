node_modules
# Keep environment variables out of version control
.env
.env.test

# The leading slash anchored this to the repo root, so it never matched the
# actual output at backend/generated/prisma -- the client was committed by
# accident. It is regenerated by `prisma generate` in CI, in Docker and locally,
# and a stale committed copy now causes type errors rather than just drift.
backend/generated/prisma

# Compiled TypeScript output.
backend/dist

.agents
.claude
skills-lock.json

# Agent work orders: already-executed task lists, stale once merged.
# The design specs under docs/superpowers/specs/ are kept on purpose.
docs/superpowers/plans/
